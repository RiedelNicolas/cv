// One place for the colours shared by the MUI theme and the canvas that paints
// the background. index.css declares the same values as custom properties for
// the plain-CSS side; keep the two in step.
export const BG = "#0d0d0f";
export const CARD = "#14141a";
export const INK = "#e8e6e3";
export const DIM = "#96938c";
export const LINE = "#2c2c35";
export const HARD = "#000000";

// The rain: RAIN_TRAIL is the body of a column, GREEN its leading glyph. GREEN
// doubles as the accent for the links and the CV button, so the page has one
// green rather than two that almost match.
export const GREEN = "#7fae8b";
export const RAIN_TRAIL = "#2c4434";
